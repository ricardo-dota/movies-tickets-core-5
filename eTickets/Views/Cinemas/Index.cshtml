@* the model *@
@model IEnumerable<Cinema>

@{
    ViewData["Title"] = "Lista de Cines";
}
<div class="row">
    <div class="col-md-8 offset-md-2">
        <table>
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Logo)</th>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>@Html.DisplayNameFor(model => model.Description)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>



                @foreach (var item in ViewBag.cinemas)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="rounded-circle" src="@item.Logo" alt="@item.Name" style="max-width: 150px" />
                        </td>
                        <td class="align-middle">
                            @item.Name
                        </td>
                        <td class="align-middle">
                            @item.Description
                        </td>

                        <td class="align-middle">
                            <a class="btn btn-outline-primary" asp-controller="Cinemas" asp-action="Edit" asp-route-id="@item.Id">
                                <i class="badge-info bi-pencil-square"></i> Edit
                            </a>
                            <a class="btn btn-outline-info" asp-controller="Cinemas" asp-action="Details" asp-route-id="@item.Id">
                                <i class="badge-info bi-pencil-eye"></i> Detail
                            </a>
                            <a class="btn btn-outline-danger" asp-controller="Cinemas" asp-action="Delete" asp-route-id="@item.Id">
                                <i class="badge-info bi-trash"></i> Delete
                            </a>
                        </td>
                    </tr>
                }


            </tbody>

        </table>
    </div>
</div>


<div class="text-white">
    <a asp-controller="Cinemas" asp-action="Create" class="btn btn-success">
        <i class="bi-pencil-eye bi-plus-circle"></i> Añadir Actor
    </a>
</div>